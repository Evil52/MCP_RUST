[package]
name = "mcp-marketplace-types"
version = "0.1.0"
edition = "2024"
rust-version = "1.98.0"
publish = false

[lints]
workspace = true

[dependencies]
schemars = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
